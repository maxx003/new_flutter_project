import 'package:flutter/material.dart';

class Settings extends StatelessWidget {
  const Settings({super.key});

  Widget build(BuildContext context) {
    return const Scaffold(body: Text('This is a Settings Page'));
  }
}
